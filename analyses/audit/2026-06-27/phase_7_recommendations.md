# Phase 7 — Recommendations · 2026-06-27

Voice: chief of staff, multi-strat fund. **Propose-only** — no file outside this audit folder is touched; this is a merged, de-duplicated patch *intention* list across the three desk lenses (buy-side PM / sell-side flow / market-maker quant).

**Provenance cap — nothing is P0 this run.** Every tier-inversion / raw≥9 data point is **pre-freeze** (the 2026-06-12 frozen rubric has produced **zero** HIGH/MEDIUM decided calls — it cannot be graded on its own outcomes), and the cross-regime picture is **partial** (only uptrend + pullback strata are decided; the choppy/selloff window 06-23..06-26 is still open). P0 requires verbatim **AND** cross-regime **AND** BH-surviving — no finding clears all three. Strongest items cap at **P1**. All rubric weight/cut changes ship **only** as pre-registrations (Phase-5 C-numbers); none has cleared its acceptance bar.

**Continuity vs 2026-06-20 (the prior Monday list):**
- *Stop single-name shorts / express bearish index-relative* → **REVISE.** This window refutes the index-relative preference: the sized real-money short book is 6 of 7 index ETFs at 33% WR / −2.10% mean, while single-name shorts ran +0.83% mean. The defensible carry-forward is the narrower "no short selection alpha in any decided regime," not an index-expression preference. (Rec 1.)
- *Bind the ≥0.80 win-rate ceiling at emission* → **CONFIRM + STRENGTHEN.** Still leaking: ≥0.80 bucket n=33 realised 42.4%, 28/33 `win_rate_source=backtest` bypass the existing cap. (Rec 3.)
- *Hold freeze + half-cap + every gate* → **CONFIRM + STRENGTHEN.** First direct protective half-cap datum (out-of-regime −16.7pp); all 9 gates net-protective. (Rec 4.)

**Freeze-lift verdict: KEEP (cannot run).** `post_freeze_decided=58` clears the count threshold, but post-freeze HIGH n=0 / MEDIUM n=0 leaves tier monotonicity ungradeable on frozen-era outcomes; raw≥9 post-freeze = 0.

---

### 1. No short selection-alpha sizing in any decided regime — and REVISE (do not strengthen) the prior index-relative prescription
**What.** Deny `swing_short` / `bearish_flow` names directional-**alpha** sizing (watch_only or pair/index-relative hedge only) — lead with the edge problem: `bearish_flow` is calibrated-but-beta (real 0.54 *beats* its 0.42 claim) yet carries no selection alpha. Explicitly do **not** encode a single-name-vs-index expression preference; the prior audit's "express bearish index-relative" rule is contradicted this window. **File.** `.claude/agents/risk-monitor.md` (size gate) + `.claude/agents/signal-confluence-quant.md` (bearish sizing note). **Phase/Data.** Phase 3b/C2: short book 47.8% vs SPY-short base 72.5% = **−24.7pp** (matched-denominator **−21.7pp**), positive in **zero** decided regimes — uptrend −34.3pp (n=35, p=0.006 SIG), pullback −8.8pp (n=34, p=0.45 NS); McNemar p=0.0035. REC-SHORT-(d): sized short book = **6 of 7 index ETFs** (SMH/IWM/SPY) at 33% WR / **−2.10% mean**, lone sized single-name (NVDA) **+3.98%**; single-name shorts +0.83% mean vs index −1.26% mean. **Priority.** P1 — short deficit is significant only in the uptrend leg and the index-vs-single-name split rests on n=1 sized single-name vs n=6 sized index (far too thin), so this is provenance-capped below P0. **Risk.** Keep it narrow: this is *no short ALPHA sizing*, not a ban on legitimate index/beta hedges. Mis-stated as "prefer index shorts" it routes the desk into exactly the names that bled (sized index ETFs); mis-stated as "prefer single-name shorts" it rests on n=1. The single-name −25.6pp excess looks worse only because those fills landed in max-down-tape windows (SPY-short base ~81% vs 50%) — a benchmark-denominator artifact, not an expression edge.

### 2. The long edge is uptrend-conditional — hold the cap, do NOT up-size longs into the undecided selloff
**What.** Treat the +21.7pp headline long excess as an **uptrend artifact**, not a standing edge; do not lift the half-cap or up-size longs on the strength of the aggregate number as the tape rolls into the window-open choppy/selloff stratum. **File.** `.claude/agents/risk-monitor.md` (do-not-up-size ruling). **Phase/Data.** Phase 3b/C3: long excess **+29.1pp** uptrend (n=55, p=0.003 SIG) collapses to **+4.9pp** pullback (n=41, p=0.79 NOT significant); choppy/selloff **n=0 decided** (18 rows, all INCONCLUSIVE/window-open, 06-23..06-26). **Priority.** P1 — single-regime; the edge is unproven cross-regime. **Risk.** If the long edge does persist cross-regime, the caution under-sizes a real edge (opportunity cost) — but the headline is uptrend-only, statistically zero in pullback, and the selloff window is ungraded; leaning on it as the tape rolls is the late-cycle trap.

### 3. Lead with the vol-class EDGE problem, then bind the ≥0.80 claimed-WR ceiling at emission (affirm C42 hygiene)
**What.** `earnings_vol` and `high_iv_rank` carry **negative edge** (calibrated-but-beta, excess ≤ 0) **and** are grossly over-claimed — stop sizing vol-class directional conviction off these quotes (edge-before-calibration). The calibration half: bind the ≥0.80 win-rate ceiling at the **emission** point so any `win_rate_source=backtest` quote from the substrate quarantined 06-12 is capped/stripped **before** it reaches the sizer. This is a win-rate-emission/substrate-hygiene fix (explicitly in-scope), **not** a frozen-rubric weight change. **File.** `.claude/agents/signal-confluence-quant.md` (win_rate emission rule) + `scripts/excess_winrate.py` (cap binding point). **Phase/Data.** Phase 3/C5: `earnings_vol` claim 0.88→real **0.38** (n=29, p<0.001, BH-surviving, excess −28.7pp) and `high_iv_rank` 0.83→**0.36** (n=11, p=0.002, BH-surviving) — the **only two** BH survivors, both vol. Phase 4: vol-structure tools are the strongest negatives (`front_end_iv_ratio` −23.1pp, `options_structure_dex` −19.8pp, `term_skew` −18.8pp). Phase 3/C6: ≥0.80 leak n=33 realised **42.4%**, **28/33** `win_rate_source=backtest`, bucket [0.80,0.90) n=27 realised 0.41, Brier 0.306, log-loss 0.852. **Priority.** P1 — both vol misses are RV-direction-proxy-resolved (not clean IV-vs-RV P&L) and the selloff regime is window-open, so the exact 0.37 is proxy-clouded → P1, not P0. **Risk.** A blunt clamp could under-size a genuinely strong setup once the substrate is re-validated cross-regime — but the substrate is **already** quarantined, so the cap only removes conviction the desk has agreed not to trust (pure downside-removal; it cannot up-size anything). Note 12/33 of the leak is real-money directional, so RV-proxy does not excuse the whole miss — the `substrate=backtest` attribution (28/33) is what holds.

### 4. Hold every protective control — freeze (lift = KEEP), half-cap (binding + protective), out-of-regime half-cap, all 9 risk gates
**What.** Do **not** lift the freeze, loosen the half-cap, relax the out-of-regime half-cap, or touch any risk gate this cycle. A do-not-change ruling, strengthened by the first direct protective half-cap datum and zero data anywhere that would justify loosening into a window-open selloff. **File.** `.claude/agents/risk-monitor.md` (hold ruling). **Phase/Data.** Phase 6: every gate net-protective (fired-WR < notfired-WR — cluster −36.5pp, panic −27.4pp, vrp −25.2pp, sector −17.5pp, regime −11.9pp). Phase 5 freeze-lift: `post_freeze_decided=58` clears the 30-count but post-freeze HIGH n=0 / MEDIUM n=0 → monotonicity ungradeable → **FREEZE_LIFT_CANNOT_RUN_KEEP_FREEZE** (post-freeze book WR 0.50 / excess −2.3pp, n=58). Half-cap **binding** (0 `full` final_size across all 257 rows; 5 `pre_risk_size=full` all capped) and **protective** (out_of_regime arm WR 0.30 / excess −16.7pp, n=10, vs in_regime 0.477 / +2.5pp, n=195). **Priority.** P1. **Risk.** The half-cap also taxes the genuine uptrend long edge (real opportunity cost if the tape re-accelerates) and the out-of-regime arm sits at exactly n=10 (the <10 thin-data floor) — but **gate discipline forbids loosening protective insurance on <10-decided data**, and relaxing insurance into a pulling-back tape is the asymmetric mistake. Treat gate magnitudes (small not-fired buckets) as advisory; signs are load-bearing.

### 5. Route the reduced long budget toward dark-pool-accumulation-gated names — the only frozen component earning its points (lean, not a promotion)
**What.** Preferentially route the (now down-sized) long risk budget to `dark_pool_accumulation`-gated names, since the accumulation toolchain is the single frozen weight that is measured-supported; keep the fundamentals-gate **live** on every accumulation-routed name. **File.** `.claude/agents/risk-monitor.md` (routing colour — qualitative lean, no scored promotion). **Phase/Data.** Phase 4/C9 + Phase 5 §1: `insights_institutional_accumulation` **+13.6pp** (n=23, most robust positive, LOAD-BEARING) behind class excess **+9.1pp** (n=33); of the four signed weights only the **+3 accumulation-conjunction** is measured-supported (`options_structure_dex` −19.8pp and `hot_chains_multileg` −12.7pp/excess 0.0 are outcome-contradicted). **Priority.** P2 — no tool survives BH for the 4th straight audit, so this is a routing lean, not a scored edit. **Risk.** Accumulation prints can mask distribution dressed as accumulation (NUVL merger-arb, NEE dividend-capture blindspots) → the fundamentals-gate must stay live; `cumulative_premium_flow` (+7.8pp) is ubiquity-confounded (cited 58% of rows) and must **not** be conflated with this signal.

### 6. Register C44 (DEX weight) + C45 (cum-flow ubiquity); carry forward C40/C41/C42; apply NONE — no tool-tier or rubric-weight change (4th BH-null audit)
**What.** Register **C44** (`+1 mechanized DEX` → reduce toward 0 / gate on a confirming-flow leg) and **C45** (`+1 cum-flow` ubiquity confound → re-measure marginal on a base-rate-discounted sub-sample, hold weight meanwhile); carry forward **C40** (multileg weight — the lie weakened to BH-null this run, weight still unjustified), **C41** (≥9 HIGH cut), **C42** (vol ≥0.80 emission ceiling — the hygiene action shipped in Rec 3). Apply **none**; make **no** tool-tier change. **File.** pre-registration register (append) — not an agent or schema file. **Phase/Data.** Phase 5: C44 DEX class-controlled marginal **−19.8pp** (n=39), with-WR collapses to 0.348 (n=23) off-uptrend vs 0.625 (n=16) uptrend; C45 cum-flow cited **120/205 = 58%** (ubiquity 0.58, base-rate confounded); C40 multileg now **BH-null** (excess 0.0, n=17, p=0.154 — was BH-surviving 0.17/n12/−38.8pp on 06-20); C41 HIGH band 0.385 < every other raw band, 3rd straight inversion, all evidence pre-freeze; **no tool clears BH at FDR 0.10** (closest `term_skew` p=0.039, `front_end_iv_ratio` p=0.050, both NEGATIVE). **Priority.** P2 — registering is zero-risk. **Risk.** The procedural risk is a **future** audit applying any of these before the bar clears — that would re-tune a frozen rubric on single-regime, BH-null, n<30/arm data (the exact failure the freeze exists to prevent). Guard the acceptance bar: cross-regime **AND** n≥30/arm **AND** BH-surviving FDR 0.10 on a future decision window.

### 7. Pre-register the schema-additive `implied_move` / entry-IV enabler (precondition for C42(b)) and a revised freeze-lift criterion that grades the bands the frozen rubric actually emits
**What.** (a) Carry `implied_move` (plus entry option mid/IV) on every vol-class call so Phase 2 can resolve the vol book on **true IV-vs-RV P&L** instead of the RV-direction proxy — this is the precondition to ever clear **C42(b)** and to judge the negative vol-structure tools on real vol outcomes rather than treating them as broken instruments. (b) Pre-register a revised freeze-lift acceptance criterion that grades the bands the frozen rubric *actually* emits (LOW-vs-DROP monotonicity + post-freeze book excess ≥ 0, cross-regime) rather than waiting on structurally empty HIGH/MEDIUM cells. **File.** `schemas/decision_envelope.schema.json` (additive `implied_move` field) + pre-registration register — additive, backward-compatible. **Phase/Data.** Phase 2 / Phase 4 §3: vol classes resolved by `rv_direction_proxy` (vol_long 100% n=11, vol_short 7.7% n=26 — a proxy signature, not a tradeable −92%); the strongest negative tools (`front_end_iv_ratio` −23.1pp, `options_structure_dex` −19.8pp, `term_skew` −18.8pp) are honestly firing on that proxy substrate, and true IV-vs-RV is unmeasurable without the field. Phase 5 freeze-lift: post-freeze HIGH n=0 / MEDIUM n=0 makes the current HIGH≥MED lift test permanent-by-construction (post-freeze book WR 0.50 / excess −2.3pp, n=58). **Priority.** P2 (enabler). **Risk.** Mild schema bloat (the field sits unused while the vol lane stays advisory) — but it is additive and lets the *next* audit grade vol edge honestly. The revised lift criterion must **keep the half-cap binding even after any future lift**, so a mis-specified LOW/DROP-only test cannot re-arm the historically-inverting HIGH band (0.143 three audits running).

### 8. Keep fractional-Kelly ADVISORY_ONLY — do not flip the sizer from the win-rate ladder to Kelly
**What.** Take **no** action to flip `signal-confluence-quant` / `risk-monitor` from the win-rate ladder to Kelly sizing; keep the live-activation gate ADVISORY_ONLY. **File.** `.claude/agents/signal-confluence-quant.md` (do-not-change ruling). **Phase/Data.** Phase 3 Kelly gate: n=23 closed < 30 floor → ADVISORY_ONLY; half-Kelly fractions all ≤0 / floored (HIGH 0.0, MEDIUM 0.013, LOW 0.0, DROP 0.0); per-tier expectancy partly RV-proxy-contaminated via vol rows (HIGH mean P&L −2.44, MEDIUM +0.10). **Priority.** P2. **Risk.** Flipping to Kelly on n=23 < 30 would size off unstable, mostly-floored fractions derived from a win-rate the same audit shows is miscalibrated (Brier 0.306) — direct oversizing risk. Keep the win-rate ladder as the live sizer until ≥30 closed trades and a clean, non-RV-proxy expectancy exist.

---

## Priority roll-up
- **P0:** none — all key evidence is pre-freeze and/or single-/partial-regime; the provenance cap forbids P0 this run.
- **P1 (act this cycle):** #1 no short alpha-sizing + revise index-relative *(EDGE)*, #2 long edge uptrend-only / don't up-size *(EDGE)*, #3 vol-class negative-edge + bind ≥0.80 ceiling at emission *(EDGE→calibration)*, #4 hold freeze + half-cap + out-of-regime cap + all 9 gates *(protective)*.
- **P2 (lean / register / enable / watch):** #5 route long budget to accumulation-gated names, #6 register C44/C45 + carry C40/C41/C42, apply none, #7 `implied_move` enabler (C42(b)) + revised freeze-lift criterion, #8 keep Kelly advisory-only.

Every item cites a real Phase-N datum + n. No tool-tier edits, no rubric weight/cut edits — only the in-scope win-rate-emission hygiene (Rec 3) and qualitative routing/hold rulings are actionable this cycle; all weight/cut changes are pre-registrations (C40–C45). No file outside `analyses/audit/2026-06-27/` is touched.
